import React from 'react';
import { View, Text, Image, ScrollView, TextInput } from 'react-native';

const scroll = () => {
    return (
        <ScrollView>
        <Text style={{fontSize: 96 }}>SCROLL - - ROLE PARA BAIXO</Text>
        <Text style={{fontSize: 96 }}>SCROLL - - ROLE PARA BAIXO</Text>
        <Text style={{fontSize: 96 }}>SCROLL - - ROLE PARA BAIXO</Text>
        <Text style={{fontSize: 96 }}>SCROLL - - ROLE PARA BAIXO</Text>
    </ScrollView>
    )
}

export default scroll;